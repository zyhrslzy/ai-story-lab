# Keep Mock mode for local development and tests. Use "ai" to call OpenAI.
STORY_GENERATOR_MODE=mock

# Required only when STORY_GENERATOR_MODE=ai.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-terra

# Use "none" to return an error instead of loading the marked Mock fallback.
STORY_GENERATOR_FALLBACK=mock
